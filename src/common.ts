const SITE_TITLE = '$STICKMAN'

const BRIEF_DESCRIPTION = 'The father of all memes.\nWe are coming.'

const FULL_DESCRIPTION =
	'Compare GitHub changelogs across multiple releases in a single view'

const AUTH_REDIRECT_STORAGE_KEY = 'auth-redirect-search-params'

const HIGH_PRIORITY_GROUP_TITLES = ['breaking changes', 'features', 'bug fixes']

const LOW_PRIORITY_GROUP_TITLES = ['others', 'credits', 'thanks', 'artifacts']

export {
	SITE_TITLE,
	BRIEF_DESCRIPTION,
	FULL_DESCRIPTION,
	AUTH_REDIRECT_STORAGE_KEY,
	HIGH_PRIORITY_GROUP_TITLES,
	LOW_PRIORITY_GROUP_TITLES,
}
